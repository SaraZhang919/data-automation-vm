name: Weekly Tuesday

on:
  schedule:
    - cron: "0 7 * * 2"   # 4 PM JST (UTC+9) every Tuesday
  workflow_dispatch:
    inputs:
      run_date:
        description: "Reference date (YYYY-MM-DD). Use a past Tuesday to test."
        required: false
        default: ""

jobs:
  weekly-tuesday:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Write Google credentials
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: |
          echo "$GOOGLE_CREDENTIALS" > /tmp/service_account.json

      - name: Run weekly page GSC
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/service_account.json
          SHEET_ID: ${{ secrets.SHEET_ID }}
          RUN_DATE: ${{ github.event.inputs.run_date }}
        run: |
          if [ -n "$RUN_DATE" ]; then
            python scripts/weekly_page_gsc.py "$RUN_DATE"
          else
            python scripts/weekly_page_gsc.py
          fi

      - name: Run weekly brand GSC
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/service_account.json
          SHEET_ID: ${{ secrets.SHEET_ID }}
          RUN_DATE: ${{ github.event.inputs.run_date }}
        run: |
          if [ -n "$RUN_DATE" ]; then
            python scripts/weekly_brand_gsc.py "$RUN_DATE"
          else
            python scripts/weekly_brand_gsc.py
          fi

      - name: Clean up credentials
        if: always()
        run: rm -f /tmp/service_account.json
